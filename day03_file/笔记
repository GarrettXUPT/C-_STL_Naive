
set/multiset：
	必须保证插入的元素是可以排序的，set/multiset会根据传入的值自动排序，set是以红黑树作为
	底层机制，查找效率高，set不允许容器中存在重复的元素，multiset允许重复元素
	set是支持迭代器的

map/multimap:
	map相对于set区别，map具有键值和实值，所有的元素根据键值自动排序，pair的第一个元素被称为键值，
	第二个元素称为实值，map也是一红黑树为底层的实现机制，将数据分装到pair中，存储到map中，当顺序不确定时，
	可以指定排序规则。
	map的迭代器不可以修改map的键值，但是可以改变map的实值
	multimap可以允许键值重复，map不允许键值重复

STL容器的共性机制：
	1、除了queue和stack以外，每个容器都提供可返回迭代器的函数，运用返回的迭代器就可以访问元素
	2、通常STL不会抛出异常，需要使用者传入正确的参数
	3、每个容器都提供一个默认的构造函数和默认的拷贝构造函数
	4、大小相关的构造方法：size() 返回容器中元素的个数 empty()判断容器是否为空

函数对象：
	重载函数调用操作符的类，其对象成为函数对象，即他们是行为类似于函数对象，也叫仿函数，其实就是重载()操作符，
	使类对象可以像函数那样被调用

	注意：
		1、函数对象(仿函数)是一个类，不是一个函数
		2、函数对象(仿函数)重载了()操作符，是它可以像函数一样被调用
	假设某一个leukemia有一个重载的operator()，而且重载的()要求获取一个参数，叫做"一元仿函数"，相反，若重载的()要求获取两个参数，这个类就是二元仿函数。

谓词：
	谓词是指普通函数或者重载的operator()返回值是bool类型的函数对象(仿函数），若operator接收一个参数，那么叫做一元
	谓词，若接收两个参数，叫做二元谓词，谓词可以作为一个判别式。
